<div class="shop-item">
          <a href="{{ $page->link }}">
                 <img src="{{ $page->image }}"/>

                 <h4>{{ $page->title }}</h4>
          </a>

          ${{ $page->price }}
</div>

           